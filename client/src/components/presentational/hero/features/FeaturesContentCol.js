import React from 'react';
import styled from 'styled-components';

const FeaturesContentCol = styled.div`
  display: grid;
  grid-template-columns: 1fr;
  justify-items: center;
  text-align: center;
`;

export { FeaturesContentCol };
