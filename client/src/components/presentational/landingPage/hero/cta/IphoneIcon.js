import React from 'react';
import styled from 'styled-components';

const IphoneIcon = styled.div`
  vertical-align: middle; /* aligns verticle relative to parent (in this case the a tag is the parent to this image) */
  margin-right: 20px;
`;

export { IphoneIcon };
